{
  "name": "@gurps-express/rules-engine",
  "version": "0.1.0",
  "description": "Pure domain contracts and rules for GURPS Express.",
  "license": "UNLICENSED",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "tsx --test src/**/tests/*.test.ts",
    "test:coverage": "tsx --test --experimental-test-coverage src/**/tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "decimal.js": "^10.6.0"
  }
}
